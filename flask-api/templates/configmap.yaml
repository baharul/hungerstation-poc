kind: ConfigMap 
apiVersion: v1 
metadata:
  name: flask-configmap 
  labels:
    {{- include "flaskapp.labels" . | nindent 4  }}
data:
  {{ .Values.config.name }}: |
    {{- toYaml .Values.config.data | nindent 4 }}