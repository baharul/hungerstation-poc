postgresql:
  auth:
    postgresPassword: secret
    username: appuser
    password: apppassword
    database: myapp  
secrets:
  data:
    user: appuser
    password: apppassword
    dbname: myapp
    dbstring: 'postgresql://appuser:apppassword@myapp-postgresql:5432/myapp'  